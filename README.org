#+title: emacs-skeleton
#+author:
#+language: en

* Overview

A template for bootstrapping new Emacs Lisp projects with sensible
defaults: tests, Makefile, documentation, and standard file structure.

Clone it, run ~init.sh~, and start building.

* Quick Start

#+begin_src sh
git clone https://github.com/YOU/emacs-skeleton my-project
cd my-project
./init.sh my-project "Your Name"
make test
git rm init.sh
git commit -am "Instantiate my-project from skeleton"
#+end_src

Or start completely fresh (no upstream history):

#+begin_src sh
git clone https://github.com/YOU/emacs-skeleton my-project
cd my-project
./init.sh my-project "Your Name" --fresh
make test
#+end_src

* What You Get

#+begin_example
.dir-locals.el                 Editor settings (indent, fill-column)
.gitignore                     Elisp, macOS, embead ignores
.github/workflows/ci.yml       GitHub Actions CI (multi-version)
LICENSE                        MIT license
Makefile                       compile, lint, test, clean
README.org                     This file (updated by init.sh)
CONTRIBUTING.org               Dev setup, code style, PR workflow
init.sh                        One-shot rename script

skeleton.el                    Entry point
skeleton-core.el               Core domain logic and data structures
skeleton-settings.el           defgroup + defcustom configuration

tests/
  skeleton-tests-runner.el     Batch + interactive test runner
  skeleton-test-helpers.el     Temp dirs, message capture macros
  skeleton-core-tests.el       Example tests
#+end_example

All files, symbols, and references are renamed by ~init.sh~.

* init.sh

#+begin_src sh
./init.sh <project-name> [author] [--fresh]
#+end_src

- Renames files: ~skeleton*.el~ → ~<project-name>*.el~
- Replaces all ~skeleton~ prefixes in code, Makefile, and docs
- Capitalizes the buffer name (~*My-Project Log*~)
- Sets author in file headers and README if provided
- Uses ~git mv~ so history stays clean
- ~--fresh~: also removes ~init.sh~, cleans references from docs, and
  resets git history with a clean initial commit

After running without ~--fresh~, delete the script and commit.

* Makefile Targets

| Target    | Description                              |
|-----------+------------------------------------------|
| ~compile~ | Byte-compile all ~.el~ files             |
| ~lint~    | Byte-compile with warnings + checkdoc    |
| ~test~    | Run tests (~SELECT=~ to filter)          |
| ~clean~   | Remove ~.elc~ files and build output     |
| ~help~    | Show available targets                   |

#+begin_src sh
make test                              # all tests
make test SELECT="^skeleton-test-core" # filter by pattern
#+end_src

* Conventions

- ~lexical-binding: t~ in every file
- Public symbols: ~prefix-name~, internal: ~prefix--name~
- Immutable structs (~:read-only t~, private constructors)
- Tests named ~prefix-test-*~, one test file per module
- Section dividers: ~;;; ─── Section Name ──────~

* Adding a Module

1. Create ~skeleton-foo.el~ with standard header
2. Add ~(require 'skeleton-foo)~ to ~skeleton.el~
3. Create ~tests/skeleton-foo-tests.el~
4. Add ~(require 'skeleton-foo-tests)~ to ~tests/skeleton-tests-runner.el~
5. Run ~make test~

* License

MIT
